# cli

This folder contains the original Node.js CLI for `clean-image`.

Run it from the repo root:

```bash
npm install --prefix cli
npm --prefix cli start
```

Direct command usage:

```bash
node cli/bin/cli.js ./image.png
```
